# Briar Invitation-to-Chat Roadmap

## Goal
- Make it possible for a user to open a Briar invitation link, add that person, and start chatting with them in one flow.

## Shared Test Inputs
- Live official-Briar invitation link for device-to-device validation on `emulator-5554` as of 2026-03-26:
  `briar://ac4rqz7nzcf656k3kqba3axajtbni4ha2jk2cfnci774pcod2ghx2`

## Agent Notes
- As of 2026-03-12, cold external launch with the live link no longer logs `Failed to accept Briar invitation` or `Briar runtime is not ready` after the repository-side readiness wait was added in `BriarContactRepository`.
- The remaining observed issue is downstream: the app still stays on `MainActivity` and does not reach chat within the current smoke window, even though the generic `Unable to add Briar contact.` failure path is no longer firing.
- When validating this link, do not classify the flow from a 6-second window alone. Check logcat and resumed activity after at least 12-15 seconds before deciding whether the remaining bug is navigation, conversation bootstrap, or pending-sync handling.
- Preferred helpers for this slice: `agent-tools/briar-peer-state.sh`, `agent-tools/briar-invitation-e2e-diagnose.sh`, and `agent-tools/invitation-link-logcat.sh`.
- The currently persisted embedded Briar account on `emulator-5554` is `hackerman` / `123456`; older notes that say `amunratis` are stale after later device/account resets.
## Definition of Done (First Slice)
- A valid invitation link can be opened from inside or outside the app.
- The app resolves the invitation through Briar contact onboarding.
- The invited user appears in people/contact surfaces with a stable canonical identity.
- A conversation is created/resolved for that contact and chat opens successfully.
- Sending the first message from that opened chat succeeds in `HYBRID` and `BRIAR_ONLY`.

## Priority Backlog (Ordered)
- [x] Hotfix blocker: resolve startup crash caused by missing Koin binding for invitation deep-link onboarding (`BriarInvitationDeepLinkEntrypoint`) in `SplashActivityViewModel`.
- [x] Define and document invitation-link contract + parser behaviour (required fields, invalid states, duplicate handling) with unit tests.
- [x] Add/verify deep-link entrypoint that routes invitation links into a dedicated onboarding action path.
- [x] Implement invitation acceptance use case in the connector/router layer (no UI-layer Briar internals).
- [x] Ensure identity mapping is persisted (`IdentityRegistry`) so accepted invites resolve to stable people/chat identity keys.
- [ ] Bootstrap or resolve conversation for accepted contact and navigate directly into chat.
- [ ] Add regression coverage for duplicate/expired/invalid invitation links and ensure clear user feedback.
- [ ] Add targeted integration tests for invitation-link onboarding to chat in `HYBRID` and `BRIAR_ONLY`.
- [x] Add a reusable QA script under `agent-tools/` for invitation-link happy-path smoke testing.

## Guardrails
- Keep Firestore usage out of this onboarding flow unless explicitly required as a temporary compatibility fallback.
- Keep Briar onboarding logic behind repository/router abstractions; avoid direct runtime calls from UI components.
- Maintain strict TDD for each backlog item (failing test first, minimal fix, tests green).

## Progress Log
- 2026-03-26: Installed the current pending-sync retry build on `emulator-5554` and reran the live 5554/5556 reciprocal baseline. `./agent-tools/briar-invitation-e2e-diagnose.sh --device emulator-5554 --skip-install --settle-seconds 20 'briar://ac4rqz7nzcf656k3kqba3axajtbni4ha2jk2cfnci774pcod2ghx2'` still cold-launched into `SignInActivity`, but after reauthenticating Arise as `hackerman` / `123456` the preserved onboarding action returned to `MainActivity` and the new retry loop stayed alive instead of discarding the invitation. On the peer side, I recovered official Briar sign-in on `emulator-5556`, opened `Pending Contact Requests`, and confirmed `ArisePeer` moved from `Connecting…` to `Waiting for contact to come online…`. Despite that reciprocal progress, Arise kept logging `awaitAddedContactIdentity timed out after 250ms`, re-entered `addContactByLink` for the same official link, and only ever surfaced the same seven `pending:` identities with no `ContactAddedEvent`, numeric contact, or `ChatActivity`. The retry fix is therefore live-validated as preserving the onboarding action, but the remaining blocker is deeper: invitation bootstrap still cannot resume from an already-existing pending-contact set after the peer is reciprocated. The next code step is TDD coverage for that promotion/resumption path rather than another generic retry-loop tweak.
- 2026-03-26: Continued the exact pending-invite continuation task with a small app-side TDD fix. The concrete requirement was that invitation onboarding must not discard its pending state after the first `ContactAddedPendingSync` result, because the reciprocal peer can complete the add later and Arise needs a way to resume conversation bootstrap automatically. I first updated `BriarInvitationOnboardingResultHandlerTest` so pending-sync feedback keeps onboarding active instead of marking it handled, then added a failing `MainActivityInvitationOnboardingTest` that returns pending sync on the first acceptance attempt and a real `conversation-42` on the second; the focused run failed because `MainActivity` never retried and no chat launch intent appeared. I then made the minimal production fix in `MainActivity`: keep the same onboarding action alive across pending-sync results, show the fallback toast only once per in-flight action, and retry acceptance after a short delay until a non-pending result arrives. Focused validation passed with `./agent-tools/test.sh --tests com.example.rise.ui.mainActivity.BriarInvitationOnboardingResultHandlerTest --timeout 900` and `./agent-tools/test.sh --tests com.example.rise.ui.mainActivity.MainActivityInvitationOnboardingTest --timeout 900`. I did not rerun the live `emulator-5554`/`emulator-5556` reciprocal baseline in this heartbeat, so the next step is to install this build on `5554` and confirm that the pending People state now promotes into chat after the official Briar peer completes the reciprocal exchange.
- 2026-03-26: Revalidated the exact live 5554/5556 invitation baseline without changing production code. The cold raw invite on `emulator-5554` still launched into `SignInActivity` with onboarding extras preserved, but Briar sign-in now failed because `AccountManagerImpl` reported `Key file does not exist`. I recovered that device state in-flow by toggling the preserved invitation screen to sign-up mode and recreating the embedded Briar account as `hackerman` / `123456`, which still forwarded the invitation onboarding action into `MainActivity`. Arise then accepted the official peer link `briar://ac4rqz7nzcf656k3kqba3axajtbni4ha2jk2cfnci774pcod2ghx2`, created pending contact `pending:c2228b0752cd090635eb78a998eebbfc6426705a77dbd35dd6a29d301f303e05`, and exposed its own raw link `briar://aajhx6g4joriedlzkmdgnivvboj2ufzi77twlwlrxnmqclwbni2qg` through the People share chooser preview. Submitting that reciprocal Arise link back into official Briar on `emulator-5556` advanced the peer to `Pending Contact Requests` for `ArisePeer`, then `Waiting for contact to come online…`. Even after the reciprocal exchange settled, `emulator-5554` stayed on `MainActivity`/People with one generic `Contact` row (`Status unknown`) and logged no renewed invitation bootstrap beyond the initial `awaitAddedContactIdentity timed out after 250ms`. The remaining blocker is therefore the Arise-side continuation from pending invite state after reciprocal exchange completes later; the next code step should be TDD coverage for resuming/promotion into confirmed contact/chat from that later background state.
- 2026-03-26: Continued the exact pending-contact bootstrap task with one small TDD fix. The concrete requirement was to stop invitation acceptance from giving up after the first deferred wait when the accepted Briar peer is already surfaced as a fresh `pending:*` contact but the confirmed numeric contact appears slightly later. I added a failing regression in `app/src/test/java/com/example/rise/transport/briar/invite/BriarInvitationAcceptanceUseCaseTest.kt` that keeps the accepted peer at `pending:fresh-3` first and only surfaces numeric contact `42` on a later retry window; the focused `./agent-tools/test.sh --tests com.example.rise.transport.briar.invite.BriarInvitationAcceptanceUseCaseTest --timeout 900` run failed because `accept()` returned with `conversationId=null` before the later retry could resolve the contact. I then made the minimal production fix in `BriarInvitationAcceptanceUseCase.bootstrapConversationIfResolvable()`: when the active identity is already `pending:*` and `awaitAddedContactIdentity()` returns null for one retry window, keep consuming the existing retry budget instead of returning pending sync immediately. Validation passed with the focused acceptance suite and the adjacent `./agent-tools/test.sh --tests com.example.rise.ui.mainActivity.BriarInvitationChatFlowBriarOnlyTest --timeout 900` suite. I did not rerun the live 5554/5556 device baseline in this heartbeat, so the next step is to install the current worktree on `emulator-5554` and confirm on the real peer baseline whether the fresh pending contact now promotes into chat.
- 2026-03-26: Continued the exact tracked 5554/5556 external-baseline task without changing product code. I re-read the heartbeat/state files, checked the tracked workspace, and recovered the blocked official Briar peer on `emulator-5556` by clearing `org.briarproject.briar.android` and recreating the account as `OfficialPeer` / `123456`. That produced a fresh peer link `briar://aaz7vkqhpan7b7dx2t3u3h62cyvwmzmxs7wa2atynk2vj62qv4gr4`. I then reran the cold external Arise flow on `emulator-5554` with `./agent-tools/briar-invitation-e2e-diagnose.sh --device emulator-5554 --skip-install --settle-seconds 15 'briar://aaz7vkqhpan7b7dx2t3u3h62cyvwmzmxs7wa2atynk2vj62qv4gr4'`. The fixed app-side cold path still went through `SplashActivity -> MainActivity -> SignInActivity`, but the first sign-in attempt exposed a stable environment fact: the persisted embedded Briar account on `5554` is `hackerman`, not the older roadmap note `amunratis`, and using `amunratis` with password `123456` fails with `IllegalStateException("Nickname does not match existing account")`. Signing in as `hackerman` / `123456` let the invitation continue, Arise exposed its reciprocal raw link `briar://adtat6wsfteaky7f65f22oseksnap3wrv5lcl3gnosjmin3v7wxea`, and submitting that link back into official Briar on `5556` produced `Pending Contact Requests` for `ArisePeer` with `Waiting for contact to come online...`. On the Arise side, logs showed `BriarContactRepository` added the fresh pending contact `pending:d4d3ac02c40a4782f8e1959f59eb07ccc884867e3b2905916d19b3783c615654`, plus `PendingContactAddedEvent` and `PendingContactStateChangedEvent(... state=OFFLINE)`, but `awaitAddedContactIdentity` still timed out and the flow stopped before confirmed-contact promotion or chat launch. The external `5556` blocker is therefore cleared; the next step is app-side TDD for the reciprocal pending-contact promotion/bootstrap path so invitation onboarding does not stop at pending-only sync when the peer is already waiting online.
- 2026-03-25: Continued the exact signed-out `MainActivity` handoff task with one small TDD-driven activity fix. I first inspected the existing `MainActivity` onboarding path and added a failing `MainActivityInvitationOnboardingTest` regression for the real device symptom: launching `MainActivity` with `BRIAR_INVITATION_ONBOARDING` while signed out should still kick off the sign-in handoff instead of leaving the activity blank. The focused `./agent-tools/test.sh --tests com.example.rise.ui.mainActivity.MainActivityInvitationOnboardingTest --timeout 900` run failed because no activity ever launched from `MainActivity`. I then made the minimal production change in `MainActivity`: keep the `viewModel.events` collector active from `onCreate` with `CoroutineStart.UNDISPATCHED` while leaving the authenticated UI and invitation-onboarding state collection under `repeatOnLifecycle(STARTED)`. The focused `MainActivityInvitationOnboardingTest` suite passed after the fix, and the adjacent `./agent-tools/test.sh --tests com.example.rise.ui.SignedOutInvitationActivityChainTest --timeout 900` suite also passed. I rebuilt/installed on `emulator-5554` and reran the same cold invite diagnostic with `./agent-tools/briar-invitation-e2e-diagnose.sh --device emulator-5554 --skip-install --settle-seconds 15 'briar://aaqazb4vgyzsltlepldcg5caw56y4rogadsr37vinguphil6o5uwc'`. The device baseline improved as intended: after `SplashActivity` and the preserved `MainActivity` onboarding action, the flow now launches `SignInActivity` again instead of stalling on blank `MainActivity`. No full 5554/5556 peer exchange happened in this heartbeat because the official Briar peer on `emulator-5556` still needs to be unlocked or re-prepared. The next step is to restore that peer baseline and rerun the full device-to-device invite path against this fixed worktree.
- 2026-03-25: Revalidated the exact cold-start invitation routing task on device without changing product code. I re-read the required heartbeat/state files, relaunched both expected AVDs, and rebuilt/installed the current worktree on `emulator-5554` with `ANDROID_SERIAL=emulator-5554 ./agent-tools/install.sh`. The intended official Briar peer baseline on `emulator-5556` came up on Briar's locked sign-in screen, and there is no stored peer password in the workspace context, so I could not finish the full live 5554/5556 peer exchange in this heartbeat. I still replayed the cold invite route on `emulator-5554` using the currently documented Briar link via `./agent-tools/briar-invitation-e2e-diagnose.sh --device emulator-5554 --skip-install --settle-seconds 15 'briar://aaqazb4vgyzsltlepldcg5caw56y4rogadsr37vinguphil6o5uwc'`. The result is materially different from the previous baseline: the cold launch starts in `SplashActivity`, logs `CompositeAuthState: isSignedIn primary=false hasDatabaseKey=false hasIdentity=false cachedIdentity=true`, then `BriarRuntimeManager: No Briar database key present during ensureStarted`, and finally resumes `MainActivity` instead of `SignInActivity`. `dumpsys activity activities` shows the task holding `com.example.rise.action.BRIAR_INVITATION_ONBOARDING` extras on `MainActivity`, so the onboarding action is preserved through splash, but after another 10-second settle window there was still no `SignInActivity`; the visible UI hierarchy was effectively blank. No product code changed in this heartbeat. The next step is to add a failing `MainActivity`-level regression for this signed-out invitation handoff and minimally restore the sign-in relay, then unlock/reprepare the official Briar peer on `emulator-5556` and rerun the full live baseline.
- 2026-03-20: Continued the exact cold-start auth/runtime investigation with one small TDD-driven splash fix. I added failing `SplashActivityViewModelTest` coverage for the real startup seam: when a cached Briar identity survives but the process is still locked, splash should probe the runtime and route a persisted-account cold start through `MainActivity` instead of going directly to `SignInActivity`. The first focused run failed because `SplashActivityViewModel` had no runtime seam, so I injected `BriarRuntimeManager` into the view model, had splash call `ensureStarted()` only when a cached local identity exists, and route to `ToMain` when the runtime then reports `hasPersistedAccount=true`. I also added a reusable `stubBriarRuntimeManager()` helper and updated the splash Robolectric test modules to bind it. Validation passed with focused `SplashActivityViewModelTest`, `SplashActivityInvitationRoutingTest`, and `SignedOutInvitationActivityChainTest` runs. This heartbeat did not yet re-run the 5554/5556 emulator baseline, so the next step is to verify the new cold-start route on device and see whether invitation onboarding now reaches `MainActivity` before the sign-in handoff.
- 2026-03-20: Continued the exact cold-start auth/runtime investigation with one small TDD-driven code change. I added failing `BriarRuntimeManagerImplTest` coverage for the case where an encrypted Briar account exists on disk but the current process has not unlocked the database key yet; the first focused run failed at compile time because `BriarRuntimeStatus` had no way to represent that state. I then added `hasPersistedAccount` to `BriarRuntimeStatus` and wired `BriarRuntimeManagerImpl` to populate it from `accountManager.accountExists()` during `ensureStarted()`, `createAccount()`, and `signIn()`. Validation passed with the focused runtime test, the full `:briar-runtime:test` suite, and a focused `CompositeAuthStateProviderTest` run in the app module. This heartbeat did not yet change startup routing on device; it only surfaced the missing persisted-account signal so the next step can update splash/auth behaviour without conflating it with the in-process `hasDatabaseKey` state.
- 2026-03-20: Continued the exact cold-start database-key restore investigation without changing product code. I inspected the embedded Briar storage and upstream account APIs instead of rerunning the same device flow. `BriarRuntimeEnvironmentImpl` still points the runtime at a stable internal `getDir("briar")`, so the cold-start loss is not coming from a rotated storage directory. The decisive finding is that upstream Briar's `AccountManager.hasDatabaseKey()` is explicitly process-local: the API docs and `AccountManagerImpl` say it only becomes true after `createAccount()` or `signIn()` in the current process and becomes false again after process exit, while disk-backed persistence is checked through `accountExists()` reading `db.key` / `db.key.bak`. Arise currently uses `runtimeStatus.hasDatabaseKey` in `CompositeAuthStateProvider` to classify cached Briar identity as signed in, and the sign-in UI only saves Firebase email/password credentials, not Briar-only credentials. That explains the reliable cold-start behaviour on `emulator-5554`: cached identity can survive while startup still reports `hasDatabaseKey=false` and routes to `SignInActivity`, so official Briar on `emulator-5556` keeps waiting for Arise to come online. The next step is to add failing coverage for the persisted-account cold-start case and introduce the minimal runtime/auth signal needed to distinguish `account exists on disk` from `database key already unlocked in this process`.
- 2026-03-20: Continued the exact startup/runtime-auth investigation without changing product code. I first inspected the relevant code paths and confirmed the earlier warm-launch log line only reported the primary-auth bit; `CompositeAuthStateProvider` can still return signed-in from cached Briar state, so a warm relaunch cannot prove whether startup really restores the Briar session. To get the trustworthy signal, I forced a true cold start on `emulator-5554` by clearing logcat, `am force-stop`-ing Arise, and relaunching `SplashActivity`. That cold launch landed directly in `SignInActivity`, and the filtered logs showed `CompositeAuthState: isSignedIn primary=false hasDatabaseKey=false hasIdentity=false cachedIdentity=true`, followed by `TransportBridge: Starting Briar runtime (mode=BRIAR_ONLY)` and `BriarRuntimeManager: No Briar database key present during ensureStarted`. So the earlier warm-launch `hasDatabaseKey=true` / `hasIdentity=true` reading was stale in-memory runtime state; the real cold-start path says Arise cannot restore a Briar database key and comes up offline. The official Briar peer on `emulator-5556` still shows `ArisePeer` in `Pending Contact Requests` with `Waiting for contact to come online…`. The next step is to inspect why the Briar database key is not restorable on cold start even though Arise still has a cached identity.
- 2026-03-20: Continued the exact reciprocal pending-state investigation without changing product code. Rechecking the official Briar peer on `emulator-5556` showed the reciprocal request had advanced from `Connecting…` to `Waiting for contact to come online…` for `ArisePeer`, which is a stronger signal that the peer is now waiting on Arise rather than still processing the submission. I then cleared Arise logcat on `emulator-5554`, relaunched the app via `SplashActivity`, waited 10 seconds, and captured the filtered invitation/runtime logs plus a fresh UI dump. Arise still did not emit any `BriarContactRepository`, `RealBriarContactService`, `BriarPeopleSync`, `ContactAddedEvent`, `PendingContactAddedEvent`, or `PendingContactRemovedEvent` lines during that fresh startup; the only app-side signal in the filtered log was repeated `CompositeAuthState: isSignedIn primary=false hasDatabaseKey=true hasIdentity=true cachedIdentity=true` while the app transitioned from `SplashActivity` back to `MainActivity`. The UI resumed on the dashboard rather than chat. The next step is to inspect why the primary/runtime session stays inactive on fresh launch even though the Briar database key and identity are present, because that now looks like the reason official Briar keeps waiting for Arise to come online.
- 2026-03-20: Continued the exact reciprocal-link/promotion validation on the intended `emulator-5554` / `emulator-5556` baseline without changing product code. On `emulator-5554`, Arise was still paused behind the Android share chooser opened from `People -> SHARE MY BRIAR LINK`, and a `uiautomator dump` of that chooser revealed the exact raw handshake link in the chooser preview text: `briar://adtat6wsfteaky7f65f22oseksnap3wrv5lcl3gnosjmin3v7wxea`. On the official Briar peer (`emulator-5556`), `AddContactActivity` was already foregrounded, so I fed that link back into official Briar with `adb -s emulator-5556 shell am start -W -a android.intent.action.VIEW -d 'briar://adtat6wsfteaky7f65f22oseksnap3wrv5lcl3gnosjmin3v7wxea' org.briarproject.briar.android`, confirmed the screen reopened with `linkInput` prefilled, advanced through `Continue`, entered nickname `ArisePeer`, and submitted `Add contact`. Official Briar then moved to `Pending Contact Requests` with `ArisePeer` shown as `Connecting…`, and it remained there after another 30-second settle window. On Arise (`emulator-5554`), dismissing the chooser returned to `MainActivity`/People with the same five `Contact` rows and `Status unknown`; targeted post-exchange logcat on 5554 still showed no `ContactAddedEvent`, `PendingContactAddedEvent`, `PendingContactRemovedEvent`, `BriarPeopleSync`, or other runtime contact refresh entries. The next step is to inspect why Arise sees no runtime contact transition while official Briar is still stuck in `Connecting…` for the reciprocal request.
- 2026-03-19: Revalidated the current worktree on the intended 5554/5556 device baseline after the stale-fallback fix landed. The official Briar peer on `emulator-5556` was already foregrounded in `AddContactActivity`, so I extracted the current `org.briarproject.briar.android:id/linkView` value (`briar://adb6gw734lfeiiw32u33nwzpczwvvxy7jp7l72f4xchicerorilvi`), rebuilt/installed Arise on `emulator-5554` with `ANDROID_SERIAL=emulator-5554 ./agent-tools/install.sh`, and reran `./agent-tools/briar-invitation-e2e-diagnose.sh --device emulator-5554 --skip-install --settle-seconds 12 'briar://adb6gw734lfeiiw32u33nwzpczwvvxy7jp7l72f4xchicerorilvi'`. The cold launch still landed in `SignInActivity`, so after `./agent-tools/invitation-link-logcat.sh --device emulator-5554 --clear` I replayed the known `hackerman` / `123456` sign-in flow. `MainActivity` resumed after reauth, and the post-reauth trace confirmed the local fix on device: after `addContactByLink(alias=Contact, pendingExists)` and `snapshotAvailableContacts` across the same five pending ids, the flow performed only a single `awaitAddedContactIdentity()` 250ms wait and returned to `MainActivity` with no repeated `Expected exactly one numeric Briar contact id` / `Briar ensureConversation failed` loop. No `ContactAddedEvent`, `PendingContactAddedEvent`, or `PendingContactRemovedEvent` appeared, the roster still contained only the same five pending identities (including `pending:547b1c00be1792da9ea4a614adb112af640bd229631c38f853ebdcdab86ed868`), and chat still did not open. No product code changed in this heartbeat. The next step is to inspect the reciprocal-link/promotion path by feeding Arise's own handshake link from `emulator-5554` back into official Briar on `emulator-5556` and checking whether that confirms the pending contact or whether contact events are still missing after both links are exchanged.
- 2026-03-19: Added focused TDD coverage for the stale post-reauth fallback branch surfaced by the fresh 5554/5556 device trace. I updated `BriarInvitationAcceptanceUseCaseTest` with a new regression where `PendingContactExistsException` does not yield a usable pending identity immediately, `ensureConversation()` fails once on the stale invite identity, and only then does a fresh `pending:` contact surface; the focused `./agent-tools/test.sh --tests 'com.example.rise.transport.briar.invite.BriarInvitationAcceptanceUseCaseTest' --timeout 180` run failed first with `expected:<pending:fresh-3> but was:<invite:inv-42>`. I then changed `BriarInvitationAcceptanceUseCase.bootstrapConversationIfResolvable()` so that after a deferred numeric-id failure with no confirmed contact yet, it re-snapshots contacts and returns pending sync as soon as a fresh `pending:` identity is observable instead of repeatedly re-entering `ensureConversation()` on the stale invite identity. Rerunning the focused invitation-acceptance suite passed, and the adjacent `./agent-tools/test.sh --tests 'com.example.rise.ui.mainActivity.BriarInvitationChatFlowBriarOnlyTest' --timeout 180` suite also passed. I did not rebuild/install or rerun the device baseline in this heartbeat. The next step is to rebuild/install on `emulator-5554`, generate a fresh official Briar invite on `emulator-5556`, and confirm the repeated numeric-contact failure is gone in the post-reauth device trace.
- 2026-03-19: Revalidated the current worktree on the intended device-to-device baseline: Arise on `emulator-5554`, official Briar peer on `emulator-5556`. `./agent-tools/briar-peer-state.sh --device emulator-5556 --launch --wait-seconds 2` confirmed the official peer at `ready_home`, I opened `Add contact at a distance` in the official Briar UI, and captured a fresh invite link from `org.briarproject.briar.android:id/linkView`: `briar://adb6gw734lfeiiw32u33nwzpczwvvxy7jp7l72f4xchicerorilvi`. `ANDROID_SERIAL=emulator-5554 ./agent-tools/install.sh` rebuilt/installed Arise, and `./agent-tools/briar-invitation-e2e-diagnose.sh --device emulator-5554 --skip-install --settle-seconds 12 'briar://adb6gw734lfeiiw32u33nwzpczwvvxy7jp7l72f4xchicerorilvi'` still cold-launched `SignInActivity` with `CompositeAuthState: isSignedIn primary=false hasDatabaseKey=false hasIdentity=false cachedIdentity=true` plus `BriarRuntimeManager: No Briar database key present during ensureStarted`. After `./agent-tools/invitation-link-logcat.sh --device emulator-5554 --clear` and replaying the known `hackerman` / `123456` sign-in flow through `./agent-tools/click.sh --file <tmp>`, Arise resumed `MainActivity`, but the post-reauth logs still repeatedly showed `TransportRouterImpl.ensureConversation()` / `BriarInvitationAcceptanceUseCase.bootstrapConversationIfResolvable()` failing with `IllegalArgumentException: Expected exactly one numeric Briar contact id`, along with repeated 250ms `awaitAddedContactIdentity()` timeouts and no `ContactAddedEvent`, `PendingContactAddedEvent`, or `PendingContactRemovedEvent`. The fresh baseline invite did surface a new fifth pending identity on `emulator-5554` (`pending:547b1c00be1792da9ea4a614adb112af640bd229631c38f853ebdcdab86ed868`), so the current blocker is now more specific: the real post-reauth bootstrap path still re-enters `ensureConversation()` while only pending identities exist. No product code changed in this heartbeat. The next step is to add or update failing invitation-bootstrap coverage for that repeated re-entry path and then make the minimal fix so numeric-contact retries stop until a confirmed Briar contact actually exists.
- 2026-03-19: Added focused TDD coverage for the exact post-reauth pending-only bootstrap bug surfaced on device. I updated `BriarInvitationAcceptanceUseCaseTest` so the `pendingExists` cases now require zero `ensureConversation()` calls while only `pending:` identities exist, confirmed the four targeted failures with `./agent-tools/test.sh --tests 'com.example.rise.transport.briar.invite.BriarInvitationAcceptanceUseCaseTest' --timeout 180`, and then changed `BriarInvitationAcceptanceUseCase.bootstrapConversationIfResolvable()` so it waits for `awaitAddedContactIdentity()` before attempting `ensureConversation()` whenever the active peer identity is already `pending:...`. The focused invitation acceptance suite passed after the fix, and the adjacent `./agent-tools/test.sh --tests 'com.example.rise.ui.mainActivity.BriarInvitationChatFlowBriarOnlyTest' --timeout 180` suite also passed. I did not rebuild/install or rerun the device flow in this heartbeat. The next step is to validate this fix on the intended device-to-device baseline: Arise on `emulator-5554`, official Briar peer on `emulator-5556`, and a fresh invite/reauth trace to confirm the immediate numeric-contact failure is gone on device.
- 2026-03-19: Completed the exact post-reauth trace after the reproduced cold external invite stall on `emulator-5554`. `./agent-tools/briar-peer-state.sh --device emulator-5554 --launch --wait-seconds 2` again reported the official Briar peer at `ready_home`, and `./agent-tools/briar-invitation-e2e-diagnose.sh --device emulator-5554 --skip-install --settle-seconds 12 'briar://acwgriblhtns4m7ogpsoh6ilspxguepwj7bezx4i4vwtwekhidgt6'` recreated the preserved Arise invite task but left the official Briar task on top. Resuming the `Rise` recents card returned to `SignInActivity`; after `./agent-tools/invitation-link-logcat.sh --device emulator-5554 --clear` and replaying the known `hackerman` / `123456` sign-in flow through `./agent-tools/click.sh --file <tmp>`, Arise resumed `MainActivity` with `com.example.rise.action.BRIAR_INVITATION_ONBOARDING` intact. The post-reauth trace still showed no `ContactAddedEvent`, `PendingContactAddedEvent`, or `PendingContactRemovedEvent`: `RealBriarContactService` refreshed four pending contacts, `addContactByLink(alias=Contact, pendingExists)` returned those same four `pending:` ids, `TransportRouterImpl.ensureConversation()` immediately failed with `IllegalArgumentException: Expected exactly one numeric Briar contact id`, and `awaitAddedContactIdentity()` timed out after 250ms. No product code changed in this heartbeat. The next step is to add a failing invitation-bootstrap regression for this post-reauth pending-only path and then make the minimal fix so onboarding stays on pending sync until a numeric Briar contact actually exists.
- 2026-03-19: Re-ran the raw external invitation flow on `emulator-5554` with the official Briar peer explicitly verified as ready first. `./agent-tools/briar-peer-state.sh --device emulator-5554 --launch --wait-seconds 2` reported the peer app at `ready_home`, then `./agent-tools/briar-invitation-e2e-diagnose.sh --device emulator-5554 --skip-install --settle-seconds 12 'briar://acwgriblhtns4m7ogpsoh6ilspxguepwj7bezx4i4vwtwekhidgt6'` still cold-launched Arise through `SplashActivity` into `SignInActivity` instead of reaching chat. The captured excerpt again logged `CompositeAuthState: isSignedIn primary=false hasDatabaseKey=false hasIdentity=false cachedIdentity=true` plus `BriarRuntimeManager: No Briar database key present during ensureStarted`, and no `ContactAddedEvent`, pending-removal transition, numeric Briar contact id, or chat launch appeared before the flow stalled. No product code changed in this heartbeat. The next step is to replay the known local Briar sign-in immediately after that reproduced cold-launch stall and capture the post-reauth trace so the next TDD fix targets the real blocker after auth handoff rather than the already-validated pending-row guard.
- 2026-03-19: Rebuilt/installed the current worktree on `emulator-5554` with `./agent-tools/install.sh`, relaunched Arise, and restored the local Briar session by replaying the existing `hackerman` / `123456` sign-in flow. After relaunching `SplashActivity`, Arise resumed `MainActivity`; the `People` tab still showed the same two `Contact` rows with `Status unknown`. I then cleared logcat, tapped the first visible pending row, and confirmed the new routing guard on device: `MainActivity` stayed resumed, the `People` UI remained visible with both pending rows still present, and the previous `ChatActivity` / `Expected exactly one numeric Briar contact id` failure did not recur. Android logged `Toast already killed. pkg=com.example.rise` immediately after the tap, which is consistent with the pending-sync message path, but I did not directly capture the toast text. No product code changed in this heartbeat. The next step is to move past the validated guard and trace why those surfaced pending contacts still never promote into confirmed numeric Briar contacts that can open chat.
- 2026-03-19: Added a failing `PeopleViewModelTest` regression for the exact pending-contact manual tap bug surfaced on device: selecting a `pending:` person from `People` previously emitted `OpenChat`, which would launch `ChatActivity` into the downstream `Expected exactly one numeric Briar contact id` failure. Fixed `PeopleViewModel.onPersonSelected()` to treat `pending:` Briar identities as pending sync and reuse the same user-facing guidance already emitted after add-by-link pending sync, then reran the focused regression and the full `PeopleViewModelTest` suite. I did not rebuild/install or rerun `emulator-5554` in this heartbeat, so the latest device evidence is still the prior tap where `ChatActivity` opened and failed on the numeric-id requirement. The next step is to rebuild/install and confirm that tapping the surfaced pending `Contact` row now stays on `People` with the pending-sync message before continuing the pending-to-confirmed promotion work.
- 2026-03-19: Rebuilt/installed and reran the external invitation flow on `emulator-5554` after the stale-pending deferred-bootstrap short-circuit landed. The raw invite still cold-launched `SignInActivity`, so I replayed the known `hackerman` / `123456` sign-in flow and then inspected resumed activity, targeted logcat, and the `People` UI. The important device change is confirmed: after `RealBriarContactService` refreshes on `addContactByLink(alias=Contact, pendingExists)`, the old 30-second bootstrap loop is gone and `BriarContactRepository` now logs only a single `awaitAddedContactIdentity` timeout at 250ms before returning to `MainActivity`. Android also logged `Toast already killed. pkg=com.example.rise`, which is consistent with the pending-sync toast path in `MainActivity`, and `./agent-tools/dump_ui.sh` now shows two `Contact` rows with `Status unknown` on the `People` screen. The invitation path still does not auto-open chat, so the next step is to inspect whether those surfaced pending contacts can already open chat manually or whether pending-to-confirmed promotion / chat routing remains the next blocker.
- 2026-03-19: Added a failing `BriarInvitationAcceptanceUseCaseTest` regression for the deferred bootstrap case where invite acceptance falls back to an already-known `pending:` identity and no confirmed Briar contact ever appears, then fixed `BriarInvitationAcceptanceUseCase.bootstrapConversationIfResolvable()` to return the pending-sync result immediately for that stale pending path instead of retrying the same `pending:` id through the full deferred budget. Revalidated the full invitation acceptance JVM suite and the onboarding-coordinator JVM suite. I did not rerun the device flow in this heartbeat, so the latest confirmed on-device evidence is still the prior run where post-reauth bootstrap looped on the same two stale pending ids. The next step is to rebuild/install and verify whether this short-circuit changes the real external invite behavior.
- 2026-03-19: Rebuilt/installed and reran the external invitation flow on `emulator-5554` after the new stale-pending `PendingContactExistsException.pendingContact` fallback landed. `./agent-tools/briar-invitation-e2e-diagnose.sh --device emulator-5554 --skip-install --settle-seconds 5 'briar://acwgriblhtns4m7ogpsoh6ilspxguepwj7bezx4i4vwtwekhidgt6'` again cold-launched `SignInActivity`; after replaying the known `hackerman` / `123456` sign-in flow through `./agent-tools/click.sh --file <tmp>`, Arise returned to `MainActivity` but still did not reach chat. Targeted logcat confirms the new fallback did not change the device outcome: `RealBriarContactService` still logs `Refreshed contacts from addContactByLink(alias=Contact, pendingExists)` with only the same two stale `pending:` ids, and `BriarInvitationAcceptanceUseCase.bootstrapConversationIfResolvable()` keeps re-running `awaitAddedContactIdentity()` / `ensureConversation()` against that unchanged pending identity for the deferred retry window before giving up. No numeric contact id, `ContactAddedEvent`, or chat launch appeared. Inference: the next TDD target is now the deferred bootstrap loop itself, not the `PendingContactExistsException.pendingContact` fallback.
- 2026-03-19: Added a failing `BriarInvitationAcceptanceUseCaseTest` regression for the swallowed `PendingContactExistsException` case where the exception already points at one of the existing stale pending ids instead of a fresh third contact, then fixed `BriarInvitationAcceptanceUseCase` to reflectively read `pendingContact.id.bytes` / `pendingContact.alias` from the thrown exception and use that specific pending identity as the bootstrap fallback after no new pending id appears in the post-exception snapshot. Revalidated the full invitation acceptance and onboarding-coordinator JVM suites. I did not rerun the device flow in this heartbeat, so the latest on-device evidence is still the previous trace where `RealBriarContactService` refreshes on `addContactByLink(alias=Contact, pendingExists)` but only resolves to the same two stale `pending:` ids. The next step is to rebuild/install and confirm whether binding to that already-known pending identity changes the real external invite flow or whether the deferred numeric-id bootstrap loop is still the user-visible blocker.
- 2026-03-19: Inspected the upstream Briar source in-tree and confirmed that `PendingContactExistsException` carries the matching `PendingContact`, then added a failing `RealBriarContactServiceTest` regression for the runtime path where `contactManager.addPendingContact()` throws that exception while `pendingContacts` is still stale. Fixed `RealBriarContactService.addContactByLink()` to catch `PendingContactExistsException`, refresh from `error.pendingContact`, and rethrow; revalidated the full runtime contact-service JVM suite and the invitation acceptance JVM suite, rebuilt/installed with `./agent-tools/install.sh`, reran the fresh-link external launch with `briar://acwgriblhtns4m7ogpsoh6ilspxguepwj7bezx4i4vwtwekhidgt6`, reauthenticated back into `MainActivity`, and pulled targeted logcat. The device trace now clearly logs `Refreshed contacts from addContactByLink(alias=Contact, pendingExists)`, which confirms the runtime-side refresh is finally happening on the real exception path, but that refresh still resolves to only the same two stale `pending:` ids and the repository still never logs `Immediate contact snapshot after addContactByLink`. Inference: the remaining blocker is no longer “missing refresh”; it is that `PendingContactExistsException.pendingContact` is likely one of those already-known stale pending contacts, so the next step is to regression-test how invitation acceptance should bind to that existing pending identity instead of waiting only for a brand-new id.
- 2026-03-19: Added a failing `BriarInvitationAcceptanceUseCaseTest` regression for the swallowed `PendingContactExistsException` case where a newly observed `pending:` identity should be preferred over the generic invite duplicate key, then fixed `BriarInvitationAcceptanceUseCase` to snapshot contacts again after that exception and use a new `pending:` id as the bootstrap fallback. Revalidated the acceptance and onboarding-coordinator JVM suites, rebuilt/installed with `./agent-tools/install.sh`, reran the fresh-link external launch with `briar://acwgriblhtns4m7ogpsoh6ilspxguepwj7bezx4i4vwtwekhidgt6`, reauthenticated back into `MainActivity`, and pulled targeted logcat. The new post-exception `snapshotAvailableContacts` call now appears on device exactly where expected, but it still returns only the same two stale `pending:` ids and there is still no `Immediate contact snapshot after addContactByLink` or `Refreshed contacts from addContactByLink(...)`. This confirms the acceptance-layer fallback is not the remaining blocker; inference: the deeper issue is now in the runtime-side `RealBriarContactService.addContactByLink()` / `contactManager.addPendingContact()` exception path before a fresh pending contact becomes visible.
- 2026-03-19: Added a failing `RealBriarContactServiceTest` regression for the stale post-submit snapshot case where `addPendingContact()` returns a fresh pending contact but `pendingContacts` still exposes only the old `FAILED` / `OFFLINE` entries, then fixed `RealBriarContactService` to surface that returned pending contact when no confirmed contacts exist yet. Revalidated the focused runtime and repository JVM suites, rebuilt/installed the app with `./agent-tools/install.sh`, and reran the fresh-link device trace with `briar://acwgriblhtns4m7ogpsoh6ilspxguepwj7bezx4i4vwtwekhidgt6`. The unit-level stale-snapshot fix held, but the on-device behavior did not change: the logs still showed `addContactByLink` on the fresh URI, still showed no `Immediate contact snapshot after addContactByLink`, and still retried `awaitAddedContactIdentity()` against the same two stale `pending:` ids. The next step is therefore deeper than the stale-return bug: inspect and regression-test the swallowed `PendingContactExistsException` path, which now looks like the more likely reason the device never surfaces a fresh pending/contact identity.
- 2026-03-19: Generated a fresh peer invite from the official Briar app instead of replaying the older shared URI. Using the official app's `Add Contact at a Distance` screen, captured `briar://acwgriblhtns4m7ogpsoh6ilspxguepwj7bezx4i4vwtwekhidgt6`, then reran the external Arise diagnosis with that fresh link and reauthenticated back into `MainActivity`. The result was effectively identical to the old-link trace: `BriarContactRepository` logged `addContactByLink` with the fresh URI, but the runtime still refreshed only the same two existing pending contacts (`PendingContactStateChangedEvent(... state=FAILED)` and `(... state=OFFLINE)`), `awaitAddedContactIdentity()` kept seeing those same two `pending:` ids, and no `PendingContactAddedEvent`, `PendingContactRemovedEvent`, or `ContactAddedEvent` arrived. This weakens the stale-link hypothesis; the next step should inspect and regression-test the embedded `addContactByLink` / pending-refresh path rather than keep rotating invitation links.
- 2026-03-19: Completed a fresh external-invite trace with the official Briar app already `ready_home`, then reauthenticated Arise from `SignInActivity` back into `MainActivity` using the repeatable `click.sh --file` flow. The fresh logcat capture showed `RealBriarContactService` refreshing the same two pending contacts via `PendingContactStateChangedEvent(... state=FAILED)` and `(... state=OFFLINE)`, `addContactByLink` submitting the live link, and `awaitAddedContactIdentity()` repeatedly seeing only those two unchanged `pending:` ids. No `ContactAddedEvent`, `PendingContactAddedEvent`, or `PendingContactRemovedEvent` arrived, and a peer-side `dump_ui.sh` check on the official Briar app still showed `Contacts` -> `No contacts to show`. The blocker is now narrower: the currently shared live invitation link may be stale/consumed or otherwise no longer producing a fresh handshake on the embedded runtime path, so the next step is to validate with a fresh invite or explicit peer-side pending-request state.
- 2026-03-19: Stayed on the pending-to-confirmed-contact investigation instead of landing another speculative code change. Inspected `BriarInvitationAcceptanceUseCase`, `TransportRouterImpl`, and the existing raw-external chat-flow tests to confirm the current contract: the onboarding path intentionally retries `ensureConversation()` 121 times on numeric-id failures before falling back to pending sync. Then ran `./agent-tools/briar-peer-state.sh --device emulator-5554 --launch --wait-seconds 2`, which confirmed the official Briar app is installed on `emulator-5554` and already in `ready_home` / authenticated state. Combined with the previous heartbeat's pending-contact visibility fix, this narrows the blocker: the missing confirmed-contact transition is not explained by the peer app being locked or unconfigured, so the next step is to capture whether Arise ever receives the `ContactAddedEvent` / pending-removal transition for the surfaced `pending:` contacts or whether the live invitation link has gone stale.
- 2026-03-19: Added a failing `RealBriarContactServiceTest` regression for `PendingContactStateChangedEvent`, confirmed the runtime roster stayed empty after a pending-contact state update, and then fixed `RealBriarContactService` to refresh contacts on pending-contact state changes instead of only logging them. Revalidated the focused runtime and repository suites, installed the new build on `emulator-5554`, and reran the live external invitation flow: the raw link still cold-launches into `SignInActivity`, but after scripted reauthentication back into `MainActivity`, `./agent-tools/invitation-link-logcat.sh --device emulator-5554` now shows `awaitAddedContactIdentity` observing two `pending:` contacts instead of `count=0`, and the People screen now renders two `Contact` rows (`Status unknown`) instead of staying empty. The slice still does not reach chat because the surfaced pending contacts never transition into confirmed contacts or a resolvable conversation during the current retry window.
- 2026-03-19: Added a portable `agent-tools/test-click.sh` regression for `agent-tools/click.sh --file`, confirmed after fixing the test's macOS-bash portability that the scripted sequence still failed for the intended reason (`Expected 3 scripted adb invocations, saw 1`), and then fixed `click.sh` by redirecting its scripted `adb shell input ...` calls from `/dev/null` so child commands no longer consume the sequence file stdin. With that repeatable reauth helper working, reran the live external invitation flow on `emulator-5554`: the raw link still cold-launches into `SignInActivity`, but the scripted sign-in now reliably resumes `MainActivity`. The downstream blocker remains unchanged on device because `./agent-tools/invitation-link-logcat.sh --device emulator-5554` still shows `BriarContactRepository.awaitAddedContactIdentity()` at `count=0` on every retry, and the People screen still shows only the share/add buttons with no pending contact row.
- 2026-03-19: Added focused pending-contact surfacing regressions in `RealBriarContactServiceTest` and `BriarContactRepositoryTest`, then updated `RealBriarContactService` to merge pending contacts from `ContactManager.getPendingContacts()` into the runtime roster with stable `pending:` IDs while `BriarContactRepository.awaitAddedContactIdentity()` keeps waiting for confirmed contacts only. Revalidated the new runtime/repository coverage plus `BriarInvitationAcceptanceUseCaseTest`, installed the build on `emulator-5554`, and reran the live external invitation diagnosis: the raw link still cold-launches into `SignInActivity` with `CompositeAuthState` showing `hasDatabaseKey=false` / `cachedIdentity=true`. A follow-up scripted reauthentication attempt did not advance past the sign-in screen, so the pending-contact surfacing fix is not yet confirmed on-device after the auth handoff.
- 2026-03-19: Updated the raw-external invitation fallback so `BriarInvitationOnboardingCoordinator` now returns the existing pending-sync continuation instead of a fatal `BriarInvitationChatLaunchFailure` when invitation acceptance succeeds but still has no conversation id; added/updated focused coordinator and Briar-only raw-link regressions first, then revalidated those suites. Installed the new build on `emulator-5554` and reran the external invitation flow with the live link: the cold launch still routes into `SignInActivity`, and after entering the existing Briar credentials (`hackerman` / `123456`) the app again reaches `MainActivity` while `BriarContactRepository.awaitAddedContactIdentity()` keeps timing out at `count=0`. The important device change is that the previous `Failed to accept Briar invitation` / `BriarInvitationChatLaunchFailure` log is gone after reauth, so the flow now degrades to pending sync on the dashboard instead of hard-failing, but it still does not reach chat because the pending contact never becomes visible as a confirmed Briar contact.
- 2026-03-19: Added a failing `BriarInvitationAcceptanceUseCaseTest` regression for the reauthenticated `PendingContactExistsException` path, updated `BriarInvitationAcceptanceUseCase` to continue invitation bootstrap instead of treating that upstream pending-contact condition as an immediate fatal failure, and revalidated the focused acceptance/coordinator/Briar-only invitation suites. Installed the new build on `emulator-5554` and reran the external invitation flow with the live link: the cold launch still routes into `SignInActivity`, and after entering the existing Briar credentials (`hackerman` / `123456`) the app now reaches `MainActivity` without the previous `PendingContactExistsException`. The slice still does not reach chat because the downstream contact bootstrap never sees a confirmed contact; `BriarContactRepository.awaitAddedContactIdentity()` stays at `count=0` on every retry, so the raw external onboarding path still ends in `BriarInvitationChatLaunchFailure`.
- 2026-03-19: Exercised the real external invitation -> `SignInActivity` Briar sign-in path on `emulator-5554` using the existing cached Briar identity (`hackerman`). After entering the existing credentials, the app launched `MainActivity` with the `com.example.rise.action.BRIAR_INVITATION_ONBOARDING` extras intact, `BriarRuntimeManager` reached the ready sign-in path, and `BriarContactRepository` reported `RealBriarContactService` as available. The invitation still did not reach chat, but the blocker moved past auth handoff: `RealBriarContactService.addContactByLink()` now fails immediately with `PendingContactExistsException`, so the next fix needs to handle the reauthenticated duplicate/pending-contact path rather than the sign-in relay itself.
- 2026-03-19: Added failing auth/runtime regressions for the stale cached-Briar-identity cold-start path, extended `BriarRuntimeStatus` with runtime session flags, updated `CompositeAuthStateProvider` to require the live runtime-backed Briar session instead of treating the persisted `IdentityRegistry` snapshot as signed-in by itself, widened the invitation log filters to include auth/sign-in routing tags, revalidated the focused `CompositeAuthStateProvider` / `RuntimeBriarAccountRepository` / `BriarRuntimeManagerImpl` JVM suites, installed the new build on `emulator-5554`, and reran the raw external invitation diagnosis. The raw invite now cold-launches `SplashActivity` -> `SignInActivity` rather than `MainActivity`, and the captured device log shows `CompositeAuthState: isSignedIn primary=false hasDatabaseKey=false hasIdentity=false cachedIdentity=true`, which confirms the earlier blocker was the cached identity being mistaken for a live Briar session. The invitation-to-chat slice still does not reach chat yet because the real device flow now stops at `SignInActivity` until the local Briar account is reauthenticated and handed through to `MainActivity`/chat.
- 2026-03-19: Added a failing `BriarRuntimeManagerImplTest` for delayed identity after `startServicesWithCurrentKey()`, updated `BriarRuntimeManagerImpl.ensureStarted()` to use the bounded identity wait there, surfaced runtime-manager messages directly to logcat, widened the invitation log filters to include runtime-manager/handle tags, installed the new build on `emulator-5554`, and reran the external invitation diagnosis. The device path changed again: after install, the raw invite now cold-launches directly into `MainActivity`, but the new logs show `BriarRuntimeManager: No Briar database key present during ensureStarted` immediately after `RealBriarContactService` is created, so services never start from a database key and `BriarContactRepository` still times out waiting for contact-service availability.
- 2026-03-19: Added failing `RealBriarContactServiceTest` coverage for immediate post-`addContactByLink` refresh and `PendingContactAddedEvent`, updated `RealBriarContactService` to refresh/log at those points, instrumented `BriarContactRepository` and `BriarPeopleSync`, widened the invitation log helpers, installed the new build on `emulator-5554`, restored the local `hackerman` / `123456` sign-in after install dropped the app back to `SignInActivity`, and reran the external invitation diagnosis. The new signed-in log trail shows the first blocker has moved earlier than contact propagation: the invite cold-launches `MainActivity`, `BriarContactRepository` starts on `NoOpBriarContactService`, observes the handoff to `RealBriarContactService`, and still times out waiting for availability because readiness never flips true within 20 seconds, so the flow fails first as `Briar runtime is not ready` before any contact-addition logs appear.
- 2026-03-19: Installed the current debug build on `emulator-5554` and reran the external invitation diagnosis. The reinstall path cold-launched `SignInActivity`, so the signed-in state had to be restored manually with the existing local Briar account (`hackerman` / `123456`). With that signed-in state restored, the raw invitation again cold-launched `MainActivity` but still failed after about 45 seconds with `BriarInvitationChatLaunchFailure`; post-failure logcat showed only the onboarding `START` intent plus the timeout failure, the People screen still showed only the share/add buttons with an empty roster, and `shared_prefs/identity_registry_store.xml` still contained only the self identity.
- 2026-03-19: Added failing invitation regressions for the post-`addContactByLink` numeric-contact handoff, updated `BriarContactRepository` and `BriarInvitationAcceptanceUseCase` to wait for the newly observed Briar contact and retry bootstrap with that resolved numeric id, carried the resolved contact identity through the onboarding/manual coordinators so launched chats message the real Briar peer, and revalidated the acceptance/coordinator/manual/Briar-only/Hybrid invitation JVM suites.
- 2026-03-19: Revalidated the signed-in external invitation flow on `emulator-5554` after current-identity priming. The raw link now cold-launches into `MainActivity` with `identity_registry_store.xml` still populated for the local Briar identity (`hackerman`), but after about 45 seconds the flow still logs `Failed to accept Briar invitation` with `BriarInvitationChatLaunchFailure`, and the People screen remains empty apart from the share/add buttons.
- 2026-03-19: Updated `BriarInvitationAcceptanceUseCase` to restore the current identity through `TransportRouter.ensureCurrentIdentity()` before `addContactByLink`; added regression coverage for the restored-identity precondition and for fast-failing before contact acceptance when identity restoration fails, then revalidated the acceptance, coordinator, and Briar-only/Hybrid invitation chat flow suites.
- 2026-03-19: Inspected on-device state after the explicit `BriarInvitationChatLaunchFailure`; the People surface showed no person rows and `shared_prefs/identity_registry_store.xml` contained `identities=[]`, so the failed invite is not surfacing in the persisted identity/People layer.
- 2026-03-19: Ran `agent-tools/briar-invitation-e2e-diagnose.sh --skip-install --settle-seconds 50` on `emulator-5554`; after about 45 seconds the signed-in external invitation flow still remained on `MainActivity` and logged `Failed to accept Briar invitation` with `BriarInvitationChatLaunchFailure`, so the device issue is now an explicit chat-launch failure rather than a short-window ambiguity.
- 2026-03-19: Added signed-in `SplashActivityInvitationRoutingTest` coverage for invitation deep links routing into `MainActivity`; it passes, which rules out the signed-in splash-to-main relay as the cause of the real-device `MainActivity` stall.
- 2026-03-19: Captured `agent-tools/invitation-link-logcat.sh` immediately after the signed-in external reproduction on `emulator-5554`; the only invitation-related line was the `MainActivity` onboarding `START` intent, with no downstream invitation/router/coordinator logs before the stall.
- 2026-03-19: Restored the existing authenticated Rise app state on `emulator-5554` and reran `agent-tools/briar-invitation-e2e-diagnose.sh --skip-install`; the raw invitation now cold-launches into `MainActivity` as intended, but it still does not reach chat within 15 seconds and logs no explicit acceptance failure.
- 2026-03-18: Ran `agent-tools/briar-invitation-e2e-diagnose.sh` with a 15-second settle window on `emulator-5554`; after reinstalling the current debug build, the raw invitation still cold-launched into `SignInActivity`, so the signed-in external invitation-to-chat slice still needs validation after restoring app auth state.
- 2026-03-18: Added `SignedOutInvitationActivityChainTest` so the signed-out local `SplashActivity` -> `SignInActivity` -> `MainActivity` invitation handoff is regression-tested end to end.
- 2026-03-18: Added `SignInActivityInvitationRoutingTest` so invitation onboarding extras are regression-tested through the real `SignInActivity` -> `MainActivity` relay after auth success.
- 2026-03-18: Added `SplashActivityInvitationRoutingTest` so signed-out invitation deep links are regression-tested through the real `SplashActivity` -> `SignInActivity` handoff.
- 2026-03-06: Added `agent-tools/invitation-link-logcat.sh` and `agent-tools/invitation-link-capture.sh` so invitation smoke runs can collect filtered logs and screenshots without ad-hoc adb commands.
- 2026-03-06: Added `agent-tools/invitation-link-smoke.sh` and a matching quick-guide section so invitation-link smoke checks can be launched consistently from local agent tools.
- 2026-03-06: Preserved the invite-link Briar alias after canonical conversation bootstrap so accepted invites keep stable lookup keys even when `TransportRouterImpl.ensureConversation()` runs, with regression coverage wired through the real router path.
- 2026-03-04: Extended `BriarInvitationAcceptanceUseCase` to optionally bootstrap a canonical conversation via `TransportRouter.ensureConversation` and return the resolved `conversationId` in `Accepted`, with regression coverage.
- 2026-03-04: Fixed invite-acceptance identity regression by ensuring `IdentityRegistryImpl.upsertIdentity(setAsCurrent = false)` never mutates `currentIdentity`; added regression coverage in `BriarInvitationAcceptanceUseCaseTest` to keep current identity unset after accepting an invite.
- 2026-03-04: Persisted accepted invitation identity mapping in `BriarInvitationAcceptanceUseCase` by upserting a deterministic canonical record in `IdentityRegistry` (duplicate key + Briar link alias), with regression unit coverage.
- 2026-03-04: Fixed startup crash (`NoDefinitionFoundException` for `BriarInvitationDeepLinkEntrypoint`) by adding Koin bindings in `App.appModule`, plus a regression test (`AppModuleInvitationBindingsTest`) to ensure the entrypoint remains resolvable.
- 2026-03-04: Added `BriarInvitationAcceptanceUseCase` in the transport invitation layer to parse deep links, delegate contact acceptance via `BriarContactRepository`, and surface accepted/invalid/failed outcomes with dedicated unit coverage.
- 2026-03-04: Added `SplashActivity` invitation `VIEW` intent filters for `arise://briar/invite` and `https://(www.)arise.app/briar/invite`, plus unit coverage for manifest contract and HTTPS onboarding action routing.
- 2026-03-04: Added `BriarInvitationLinkParser` contract + unit coverage and documented the supported invitation link shapes in `docs/briar_invitation_link_contract.md`.
- 2026-03-04: Roadmap created and set as active execution plan for autowork.
- 2026-03-04: Added Splash deep-link routing for invitation links into a dedicated `MainActivity` onboarding action payload (`BriarInvitationOnboardingAction`) with unit coverage in `SplashActivityViewModelTest`.
- 2026-03-06: Added `agent-tools/invitation-link-artifacts.sh` as invitation artifact locator helper.
- 2026-03-06: Added `agent-tools/invitation-link-prefix.sh` and documented it in `docs/invitation_artifact_toolkit.md`.
- 2026-03-06: Added `agent-tools/invitation-link-bundles.sh` and documented bundle lookup in `docs/invitation_artifact_toolkit.md`
- 2026-03-06: Added `agent-tools/invitation-link-log-tail.sh` and documented usage in `docs/invitation_artifact_toolkit.md`

2026-03-06: Added \`agent-tools/invitation-link-summary.sh\` as invitation artifact summary helper
- 2026-03-06: Added `agent-tools/invitation-link-log-search.sh` and documented targeted invitation log search in `docs/invitation_artifact_toolkit.md`.

- 2026-03-06: Added agent-tools/invitation-link-report.sh and documented latest bundle reporting in docs/invitation_artifact_toolkit.md

- 2026-03-06: Added  and documented latest bundle JSON output in .
- 2026-03-06: Added `agent-tools/invitation-link-json.sh` and documented latest bundle JSON output in `docs/invitation_artifact_toolkit.md`.
- 2026-03-11: Added `agent-tools/install-briar-direct.sh` to download the official Briar direct APK and install it on the emulator for repeatable external-app testing of the invitation-to-chat flow.
- 2026-03-11: Added `agent-tools/briar-peer-state.sh` so overnight runs can classify whether the external Briar peer app is missing, stuck on onboarding, locked, or ready before relying on it for end-to-end testing.
- 2026-03-11: Updated `MainActivity` deep-link invitation onboarding feedback so classified duplicate, expired, and invalid Briar rejections surface specific user messages, with focused result-handler regression coverage.
- 2026-03-11: Updated `BriarInvitationOnboardingCoordinator` to launch chat even when invite acceptance returns no `conversationId`, with regression coverage for deferred bootstrap launch.
- 2026-03-11: Restored the safe pending-sync onboarding result when invite acceptance still returns no `conversationId`, with regression coverage for deferred bootstrap that never resolves.
- 2026-03-11: Extracted `MainActivity` invitation onboarding result handling into a dedicated helper so direct chat launch and the preserved pending-sync fallback are both regression-tested at the activity handoff boundary.
